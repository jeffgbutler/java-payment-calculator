greeting: Hello Tanzu!!

info:
  app:
    name: Payment Calculator

management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: "*"
  metrics:
    export:
      wavefront:
        enabled: false
