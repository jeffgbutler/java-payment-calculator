greeting: Hello TAP!

info:
  app:
    name: Payment Calculator

management:
  endpoint:
    health:
      show-details: always
    info:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
